name: Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake penangdriver
        run: curl -fsS https://penangdriver.onrender.com/health || true
      - name: Wake my-hub (public)
        run: curl -fsS https://my-hub.onrender.com/health || true
